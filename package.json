{
  "name": "transitionable",
  "version": "1.1.0",
  "description": "Small library providing a spring observable",
  "main": "dist/transitionable.min.js",
  "typings": "src/index.ts",
  "repository": "https://github.com/aitoroses/transitionable-js",
  "author": "grayfox",
  "license": "MIT",
  "scripts": {
    "start": "run-p open server",
    "open": "sleep 2 && open http://localhost:8080/example/mouse.html",
    "server": "node ./fuse.js --server",
    "build:dev": "node ./fuse.js",
    "build:prod": "NODE_ENV=production node ./fuse.js",
    "build": "run-p build:*",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "fuse-box": "^1.3.109",
    "npm-run-all": "^4.0.1",
    "source-map": "^0.5.6",
    "typescript": "^2.1.6",
    "uglify-js": "^2.7.5"
  },
  "dependencies": {
    "raf.js": "^0.0.4"
  }
}
